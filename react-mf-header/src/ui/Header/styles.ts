import styled from "styled-components";

export const StyledHeader = styled.nav`
  display: flex;
  height: 5vh;
  box-sizing: border-box;
  align-items: center;
  background-color: lawngreen;
  box-shadow: 0px 2px 5px 0 lawngreen;
  padding: 8px 32px;
  gap: 24px;
  opacity: 0.8;
`;

export const StyledNavButton = styled.button`
  cursor: pointer;
  background: inherit;
  font-family: inherit;
  border: none;
  padding: 8px 12px;
`;
